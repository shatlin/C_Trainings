// Explain and Declare
1) int (*p(char *))[10];
2) int* (*p[10])(char *);
3) Declare a pointer to a function that accepts an argument which
   is a pointer to an integer array and returns a void pointer.
4) Declare a pointer to an interger array of fifty elements.
